#pragma once

#include <cstddef>
namespace snek {
    constexpr std::size_t INITIAL_SCREEN_WIDTH = 1280,
                          INITIAL_SCREEN_HEIGHT = 720, PADDING = 75,
                          BOARD_HEIGHT = 10, BOARD_WIDTH = 20,
                          MOVE_SPEED = 250000;
}