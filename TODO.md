fps
maybe change all std::size_t to int where it makes sense
includes in cpp file if possible
compiler options for size / speed optimisations?
separate input functions
make all casts static casts
change "sdl.h" to <sdl.h>
move std::shared to unique
add startup screen explaining what to do
speed slider + pause button 
optimize algorithm to solve caves
optimize algorithm to go up to the top of the vertical line instead of going in the middle of a vertical line and screwing itself over
random hamiltonian cycle on each run?
auto restart when hamiltonian cycle is over